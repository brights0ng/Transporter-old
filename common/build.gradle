architectury.common(forge_enabled.toBoolean())

dependencies {
    modImplementation "net.fabricmc:fabric-loader:$fabric_loader_version"
    modImplementation "dev.lazurite:toolbox-common:$toolbox_version", { transitive false }
}